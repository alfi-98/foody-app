import { View, Text } from 'react-native'
import React from 'react'

const Test = ({ key, id, name, description, price, image }) => {
  return (
    <View>
      <Text>Test</Text>
    </View>
  )
}

export default Test